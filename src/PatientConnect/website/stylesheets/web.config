<?xml version="1.0" encoding="utf-8"?>

<!-- allow everyone to get stylesheets, even if not logged in -->
<configuration>
    <system.web>
        <authorization>
            <allow users="*" />
        </authorization>
    </system.web>
</configuration>